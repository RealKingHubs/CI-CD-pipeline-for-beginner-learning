name: Practice Workflow
on:
  push:
    branch:
      - main

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: CheckOut Repository
        uses: actions/checkout@v4
 
      - name: Say hello
        run: echo "Hello, Kingsley"
